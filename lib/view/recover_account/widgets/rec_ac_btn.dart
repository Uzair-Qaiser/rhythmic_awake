import '../../../widgets/custom_btn.dart';
import 'package:flutter/material.dart';
class RecoverAcBtn extends StatelessWidget {
  const RecoverAcBtn({super.key});
  @override
  Widget build(BuildContext context) {
    return CustomBtn(text: 'Continue',);
  }
}
